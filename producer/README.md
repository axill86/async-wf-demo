# Producer application
Sample producer application for minicompute

## How to build
golang is required. Build can be done with following command:  
`go build `  

After application is build, usage can be printed with the following command  
`./producer`